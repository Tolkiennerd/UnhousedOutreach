import { LookupContextType } from "../";

export interface LookupsContextType
{
  cushionCondition: LookupContextType;
  cushionType: LookupContextType;
  disability: LookupContextType;
  ethnicity: LookupContextType;
  gender: LookupContextType;
  housingStatus: LookupContextType;
  locationType: LookupContextType;
  pantsSize: LookupContextType;
  request: LookupContextType;
  shirtSize: LookupContextType;
  shoeSize: LookupContextType;
  sleepingBagCondition: LookupContextType;
  tentCondition: LookupContextType;
  tentUsage: LookupContextType;
}